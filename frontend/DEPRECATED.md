# DEPRECATED

This `frontend/` directory has been replaced by a Python FastAPI + Jinja2 + HTMX + Tailwind app at the repository root.

To clean up:

```bash
git rm -r frontend/
git commit -m "chore: remove deprecated Next.js frontend"
```
